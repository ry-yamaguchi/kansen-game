# 感染るラボ — 実装者（人間/AI）への指示

ブラウザで遊ぶ、感染の広がりを食い止めるリアルタイム介入ゲーム。
**「眺めるシミュレーション」ではなく「どこに、いつ介入するか」を判断させる**のが狙いである。

- 応答・コミットメッセージ・UI文言は**すべて日本語**で書く。
- 横断の原則は `~/dev-playbook/principles/` にある。着手前に `git -C ~/dev-playbook pull` して読む。
- 現在地と優先順位は [docs/roadmap.md](docs/roadmap.md)。**着手前に読む。**
- 作者の要望は [docs/backlog.md](docs/backlog.md)、外からの意見は [docs/feedback.md](docs/feedback.md)。
- ここの掟の番号は他文書から参照される。**番号を変えないこと。**

## 経緯

2026-09-17 に Kiro（spec駆動のIDE）で MVP を作り、公開した。
Kiro での開発はその日で終わり、**2026-09-23 から Claude Code が引き継いでいる。**
`.kiro/` には当時の指針と自動処理が残っているが、**いまの正はこの CLAUDE.md である。**
[AGENTS.md](AGENTS.md)（外部の協力者とそのAIエージェント向けの窓口）は生きている。

---

## 共通の掟（全作業に適用）

### 1. 遊びの核を壊さない

新しい要素は「**判断を濃くするか、薄めるか**」で採否を決める。薄めるものは入れない。

2026-09-23 に、放置すると崩壊し、上手く打てば守れるように直した。
現在値と残課題は [docs/roadmap.md](docs/roadmap.md) にある。

**盤面を読まずに打てる手が最善になっていないか**を、変更のたびに `npm run balance` で見張る。
（かつては「ワクチンだけ連打」「隔離を所構わず置く」が最善手に近かった。
2026-09-26 には、感染者が必ず駅から来たため「駅に隔離を置き直し続けるだけ」が本気AIより強かった。
脅威が決まった場所から来る仕組みを足したら、そこに張り付く打ち方を計測に足す。`BALANCE_ONLY=station` で見張る）

### 2. シミュレーションは純粋・決定論に保つ

`src/sim/` は純粋関数だけで書く。DOM・Canvas・`Date.now()`・`Math.random()` を呼ばない。
乱数はシード付きで、`SimState` が持つ `rng` から取る。乱数の発生源は UI 側に置く。

**同じシード＋同じ操作列 → 同じ結果**が崩れると、バランス調整もテストも成立しない。
（2026-09-17 の実装は `Math.random()` を直接呼んでおり、どの変更が効いたのか測れなかった）

### 3. バランスは測ってから変える

数値を勘で動かさない。

```bash
npm run balance   # ヘッドレスで各戦略を回し、抑えた割合・社会活動・スコアを出す
```

**変更の前後で必ず両方を取り、差を見る。** シードは固定なので、同じ条件なら同じ数字が出る。

ただし**20試合では崩壊率の比較に足りない**（誤差±10%ほど）。初期状態を変える変更は乱数の
消費順がずれて別の20試合になるため、前後で別のサンプルを比べることになる。
崩壊率で判断するときは100試合で取る。

計測は PC とスマートフォンの両方の盤面で取る。画面の形で別のゲームになっていないかを見張るため。

### 4. 検証

```bash
npm test          # ロジックを足したらテストも足す。合否は Tests の行で見る（tail で切らない）
npm run build
```

テストは**わざと壊して落ちるか**を確かめる。落ちないテストは無いのと同じである。

### 5. コミット

author は `meryo <meryo2000@gmail.com>`（このリポジトリの git config に設定済み。
**グローバルは仕事用なので `--global` を付けて上書きしないこと**）。
`Co-Authored-By` 行は付けない。コミット履歴は書き換えない（rebase・force push をしない）。
`git add -A` を避け、commit の前に `git status --short` を目で見る。

### 6. 公開

main へ push すると GitHub Actions が <https://ry-yamaguchi.github.io/kansen-game/> へ自動公開する。
文書だけの変更では走らない（`paths-ignore`）。Vite の `base` は `/kansen-game/` でなければ
公開URLでアセットが 404 になる。

### 7. 止まっているときは資源を使わない

一時停止・タブ非表示・画面離脱で `requestAnimationFrame` とタイマーを止める。
放置して重くならないこと。

### 8. 文体

| 対象                                     | 文体                 |
| ---------------------------------------- | -------------------- |
| コメント、設計文書、コミットメッセージ   | 「である」調         |
| **画面に出る文言、エラーメッセージ**     | **「です・ます」調** |

失敗を伝えるときは、原因と次にとれる行動を添える。

### 9. 実在のものを題材にしない

舞台・病名・団体名はすべて架空である。現実の感染症を予測・再現するものではない。

---

## 構成

```
src/
  sim/          シミュレーション中核（純粋・決定論・描画に依存しない）
    types.ts      状態の型
    config.ts     バランス定数（数値はここに集める）
    engine.ts     1ティックの計算、介入の適用、スコア算出
    modes.ts      感染症／噂話／悪感情のモード定義と用語・イベント
    rng.ts        シード付き乱数
  render/       Canvas 描画（draw.ts・characters.ts・view.ts）
  components/   画面（Hud・ActionBar・StartScreen・ResultScreen）
  hooks/        useGame.ts — ループと状態の受け渡し
scripts/
  balance.ts    ヘッドレスのバランス計測
  shot.mjs      スクリーンショット取得
```

**`ui`（components/render）から `sim` は呼んでよいが、逆は呼ばない。**
ゲームのルールを画面側に書かない。

## 進め方

- **実装はサブエージェント（Sonnet）へ委譲する。** メインは仕様策定・設計判断・検証に徹する。
- 委譲の前に、触るファイル・期待する振る舞い・制約・完了条件・流すコマンドを固める。
- 子にコミットさせない。**差分とテスト結果を自分で確かめてから**報告する。
- 最も安い決定的な検証から。作者の観察は事実として扱い、まず棚卸しする。
